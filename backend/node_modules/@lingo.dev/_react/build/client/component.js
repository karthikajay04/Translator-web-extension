"use client";
import { jsx } from "react/jsx-runtime";
import {
  LingoComponent as LingoCoreComponent
} from "../core/index.js";
import { useLingo } from "./context.js";
export function LingoComponent(props) {
  const { $as, $fileKey, $entryKey, ...rest } = props;
  const lingo = useLingo();
  return /* @__PURE__ */ jsx(
    LingoCoreComponent,
    {
      $dictionary: lingo.dictionary,
      $as,
      $fileKey,
      $entryKey,
      ...rest
    }
  );
}
export function LingoHtmlComponent(props) {
  const lingo = useLingo();
  return /* @__PURE__ */ jsx(
    "html",
    {
      ...props,
      lang: lingo?.dictionary?.locale,
      "data-lingodotdev-compiler": lingo?.dictionary?.locale
    }
  );
}
