import { getDictionary } from "../core/index.js";
export const loadDictionary = async (locale) => {
  return {};
};
export const loadDictionary_internal = async (locale, dictionaryLoaders = {}) => {
  return getDictionary(locale, dictionaryLoaders);
};
