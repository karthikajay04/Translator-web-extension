import { jsx } from "react/jsx-runtime";
import { LingoProvider as LingoClientProvider } from "../client/index.js";
import { loadDictionaryFromRequest } from "./utils.js";
export async function LingoProvider(props) {
  const dictionary = await loadDictionaryFromRequest(props.loadDictionary);
  return /* @__PURE__ */ jsx(LingoClientProvider, { dictionary, children: props.children });
}
