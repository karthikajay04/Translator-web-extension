import { jsx } from "react/jsx-runtime";
import {
  LingoAttributeComponent as LingoCoreAttributeComponent
} from "../core/index.js";
import { loadDictionaryFromRequest } from "./utils.js";
export async function LingoAttributeComponent(props) {
  const {
    $attrAs,
    $attributes,
    $fileKey,
    $entryKey,
    $loadDictionary,
    ...rest
  } = props;
  const dictionary = await loadDictionaryFromRequest($loadDictionary);
  return /* @__PURE__ */ jsx(
    LingoCoreAttributeComponent,
    {
      $dictionary: dictionary,
      $as: $attrAs,
      $attributes,
      $fileKey,
      ...rest
    }
  );
}
